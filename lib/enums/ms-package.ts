export enum MsPackageCategory {
  MONTHLY_PREMIUM = "monthly_premium",
  LIFETIME_PREMIUM = "lifetime_premium",
  LIFETIME_FREE = "lifetime_free",
}

export enum PurchaseStatus {
  PENDING = "pending",
  SUCCEEDED = "succeeded",
  FAILED = "failed",
}

export enum PackageStatus {
  ACTIVE = "active",
  INACTIVE = "inactive",
}

export enum AccountType {
  ALL = "all",
  PREMIUM = "premium",
}
